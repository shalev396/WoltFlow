name: Extension CI/CD

permissions:
  id-token: write
  contents: read

on:
  push:
    branches:
      - 'main' # Production deployment to Chrome Web Store
      - 'dev' # Development build and validation
    paths:
      - 'Extension/**'
      - '.github/workflows/Extension-CICD.yml'

jobs:
  build-and-deploy-extension:
    name: Build and Deploy Extension
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

    #   - name: Use Node.js
    #     uses: actions/setup-node@v4
    #     with:
    #       node-version: 22

    #   - name: Setup Python for icon generation
    #     uses: actions/setup-python@v4
    #     with:
    #       python-version: "3.11"

    #   - name: Install Python dependencies
    #     run: |
    #       python -m pip install --upgrade pip
    #       pip install Pillow cairosvg
    #     working-directory: Extension

    #   - name: Generate extension icons
    #     run: python create_icons.py
    #     working-directory: Extension

    #   - name: Install Node.js dependencies
    #     run: npm install
    #     working-directory: Extension

    #   - name: Validate extension
    #     run: npm run validate
    #     working-directory: Extension

    #   - name: Package extension
    #     run: npm run package-windows
    #     working-directory: Extension

    #   - name: Load environment variables from ENV
    #     run: |
    #       echo '${{ secrets.ENV }}' > .env
    #     working-directory: Extension

    #   - name: Export .env variables
    #     uses: falti/dotenv-action@v1.1.4
    #     with:
    #       path: Extension/.env
    #       export-variables: true
    #       keys-case: bypass
    #       mask-variables: true

    #   - name: Deploy to Chrome Web Store Dev
    #     if: github.ref == 'refs/heads/dev'
    #     uses: mnao305/chrome-extension-upload@v5.0.0
    #     with:
    #       file-path: Extension/woltflow-token-reviewer.zip
    #       extension-id: ${{ env.CHROME_EXTENSION_ID_DEV }}
    #       client-id: ${{ env.CHROME_CLIENT_ID }}
    #       client-secret: ${{ env.CHROME_CLIENT_SECRET }}
    #       refresh-token: ${{ env.CHROME_REFRESH_TOKEN }}
    #       publish: false

    #   - name: Upload development build artifact
    #     if: github.ref == 'refs/heads/dev'
    #     uses: actions/upload-artifact@v4
    #     with:
    #       name: woltflow-extension-dev
    #       path: Extension/woltflow-token-reviewer.zip
    #       retention-days: 30

    #   - name: Deploy to Chrome Web Store Prod
    #     if: github.ref == 'refs/heads/main'
    #     uses: mnao305/chrome-extension-upload@v5.0.0
    #     with:
    #       file-path: Extension/woltflow-token-reviewer.zip
    #       extension-id: ${{ env.CHROME_EXTENSION_ID_PROD }}
    #       client-id: ${{ env.CHROME_CLIENT_ID }}
    #       client-secret: ${{ env.CHROME_CLIENT_SECRET }}
    #       refresh-token: ${{ env.CHROME_REFRESH_TOKEN }}
    #       publish: true

    #   - name: Upload production build artifact
    #     if: github.ref == 'refs/heads/main'
    #     uses: actions/upload-artifact@v4
    #     with:
    #       name: woltflow-extension-prod
    #       path: Extension/woltflow-token-reviewer.zip
    #       retention-days: 90

    #   - name: Create GitHub Release
    #     if: github.ref == 'refs/heads/main'
    #     uses: actions/create-release@v1
    #     env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     with:
    #       tag_name: extension-v${{ github.run_number }}
    #       release_name: WoltFlow Extension v${{ github.run_number }}
    #       body: |
    #         🌊 WoltFlow Token Reviewer Extension Release

    #         **Features:**
    #         - Extract access and refresh tokens from Wolt.com
    #         - Beautiful gradient UI matching WoltFlow branding
    #         - Secure local processing - no data transmission
    #         - Modern Chrome Manifest V3 architecture

    #         **Installation:**
    #         1. Download the extension package
    #         2. Extract the files
    #         3. Load in Chrome via chrome://extensions/ (Developer mode)

    #         **Auto-deployed to Chrome Web Store** 🚀
    #       draft: false
    #       prerelease: false

    #   - name: Upload release asset
    #     if: github.ref == 'refs/heads/main'
    #     uses: actions/upload-release-asset@v1
    #     env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     with:
    #       upload_url: ${{ steps.create_release.outputs.upload_url }}
    #       asset_path: Extension/woltflow-token-reviewer.zip
    #       asset_name: woltflow-token-reviewer.zip
    #       asset_content_type: application/zip
